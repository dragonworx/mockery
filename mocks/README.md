# Mocks Directory

This directory contains files copied from user uploads for serving by the HTTP Request Mocker extension.

Files are automatically copied here when users upload them through the extension popup, and are served using relative paths for security and portability.

## Structure
- Files maintain their original names with optional timestamp prefixes to avoid conflicts
- Subdirectories can be created for organization
- All paths are recorded relative to this mocks directory